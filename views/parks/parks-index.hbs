<div class="container coasters-index">

    <a href="/parks/" class="btn btn-dark">Crear un nuevo parque</a>

    <h1 style="font-weight: 100">Índice de Parques</h1>

    <hr>
    {{#each allParks}}
    <a href="/parks/detalle/{{name}}" class="btn btn-sm btn-dark">Ver detalles</a>
    <a href="/coasters/editar?coasterId={{_id}}" class="btn btn-sm btn-dark">Editar</a>
    <h4>{{name}}</h4>
    <p>{{description}}</p>


    <hr>
    {{/each}}
</div>